[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "alembic upgrade head && echo 'Migrations done, starting uvicorn on port '$PORT && uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
